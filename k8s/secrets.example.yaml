# Template only - do not apply as-is.
# Generate a real random value, e.g.: openssl rand -hex 16
# Then follow the project's sealed-secrets pattern: printf '%s' '<value>' | kubeseal --scope cluster-wide ...
apiVersion: v1
kind: Secret
metadata:
  name: samba-secrets
  namespace: samba
type: Opaque
stringData:
  smb-password: "REPLACE_ME"
